{
  "name": "jawt",
  "description": "A WebCrypto based JSON Web Token implementation without dependencies",
  "license": "MIT",
  "version": "0.0.8",
  "main": "./src/index.js",
  "exports": "./src/index.js",
  "type": "module",
  "types": "./dist/index.d.ts",
  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  },
  "engines": {
    "node": ">=15"
  },
  "repository": {
    "url": "git@github.com:ksmithut/jawt.git"
  },
  "homepage": "https://github.com/ksmithut/jawt",
  "files": [
    "src/**/*.js",
    "dist/**/*.ts"
  ],
  "scripts": {
    "format": "prettier-standard",
    "lint": "prettier-standard --check --lint && tsc --noEmit",
    "clean": "node -e 'fs.promises.rm(\"dist/\", { force: true, recursive: true })'",
    "prebuild": "npm run clean",
    "build": "tsc --emitDeclarationOnly",
    "prepublish": "npm run build",
    "test:node": "NODE_V8_COVERAGE=coverage/ node --test test/",
    "test": "tap --no-coverage --reporter=spec test/",
    "test:coverage": "c8 --reporter=html --reporter=text --reports-dir=./coverage/lcov-report -- npm run test",
    "prepare": "husky install || true"
  },
  "dependencies": {},
  "devDependencies": {
    "@ksmithut/prettier-standard": "^0.1.0",
    "@types/node": "^17.0.32",
    "c8": "^7.11.2",
    "husky": "^8.0.1",
    "jsonwebtoken": "^8.5.1",
    "lint-staged": "^12.4.1",
    "standard": "^17.0.0",
    "tap": "^16.2.0",
    "typescript": "^4.6.4"
  }
}
