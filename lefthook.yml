pre-commit:
  parallel: true
  commands:
    formatter:
      glob: '*.*'
      run: prettier-standard --lint {staged\_files}
    types:
      glob: '*.{js,json}'
      run: tsc --emitDeclarationOnly
