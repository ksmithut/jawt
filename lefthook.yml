pre-commit:
  parallel: true
  commands:
    formatter:
      glob: '*.*'
      run: yarn prettier-standard --lint {staged_files}
    types:
      glob: '*.{js,json}'
      run: yarn tsc --emitDeclarationOnly
