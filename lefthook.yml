pre-commit:
  parallel: true
  commands:
    formatter:
      glob: '*.*'
      run: yarn prettier-standard --lint {staged\_files}
    types:
      glob: '*.{js,json}'
      run: yarn tsc --emitDeclarationOnly
